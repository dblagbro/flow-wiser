From e0e4392f25102f76d4384f112d3947b6ee8e589d Mon Sep 17 00:00:00 2001
From: AbhilashG12 <abhilashggg15@gmail.com>
Date: Fri, 19 Jun 2026 18:18:27 +0530
Subject: [PATCH 1/2] fix: prevent duplicate processes on ChatOpenAI timeout

---
 .../components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts   | 7 ++++++-
 1 file changed, 6 insertions(+), 1 deletion(-)

diff --git a/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts b/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts
index 03c13fee9b0..5b4edbdb45f 100644
--- a/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts
+++ b/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts
@@ -255,7 +255,12 @@ class ChatOpenAI_ChatModels implements INode {
         if (topP) obj.topP = parseFloat(topP)
         if (frequencyPenalty) obj.frequencyPenalty = parseFloat(frequencyPenalty)
         if (presencePenalty) obj.presencePenalty = parseFloat(presencePenalty)
-        if (timeout) obj.timeout = parseInt(timeout, 10)
+        if (timeout) {
+            obj.timeout = parseInt(timeout, 10)
+            // If a timeout is explicitly set, prevent LangChain from silently retrying
+            // the request after it aborts, which causes duplicate zombie processes.
+            obj.maxRetries = 0
+        }
         if (cache) obj.cache = cache
         if (stopSequence) {
             const stopSequenceArray = stopSequence.split(',').map((item) => item.trim())

From a253d6e047c34de1b271d84401c50602afdebb68 Mon Sep 17 00:00:00 2001
From: AbhilashG12 <abhilashggg15@gmail.com>
Date: Fri, 19 Jun 2026 18:27:38 +0530
Subject: [PATCH 2/2] chore: add validation for parsed timeout value

---
 .../nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts         | 11 +++++++----
 1 file changed, 7 insertions(+), 4 deletions(-)

diff --git a/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts b/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts
index 5b4edbdb45f..bfe8be53f9e 100644
--- a/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts
+++ b/packages/components/nodes/chatmodels/ChatOpenAI/ChatOpenAI.ts
@@ -256,10 +256,13 @@ class ChatOpenAI_ChatModels implements INode {
         if (frequencyPenalty) obj.frequencyPenalty = parseFloat(frequencyPenalty)
         if (presencePenalty) obj.presencePenalty = parseFloat(presencePenalty)
         if (timeout) {
-            obj.timeout = parseInt(timeout, 10)
-            // If a timeout is explicitly set, prevent LangChain from silently retrying
-            // the request after it aborts, which causes duplicate zombie processes.
-            obj.maxRetries = 0
+            const parsedTimeout = parseInt(timeout, 10)
+            if (!isNaN(parsedTimeout) && parsedTimeout > 0) {
+                obj.timeout = parsedTimeout
+                // If a timeout is explicitly set, prevent LangChain from silently retrying
+                // the request after it aborts, which causes duplicate zombie processes.
+                obj.maxRetries = 0
+            }
         }
         if (cache) obj.cache = cache
         if (stopSequence) {
