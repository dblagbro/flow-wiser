# Upstream delta — 2026-08-12

Refresh of the 2026-08-05 snapshot. **The premise of that snapshot no longer holds**, which is the
most important thing in this document.

## Upstream is not archived, and it is still merging

`MANIFEST.md` was captured because "upstream repository moves to public archive on 2026-08-10,
which locks issues and pull requests." Verified today against the GitHub API:

| Fact | Value |
| --- | --- |
| `archived` | **`false`** |
| `disabled` | `false` |
| Last push to code | 2026-08-10T14:56Z |
| Repo `updated_at` | 2026-08-12T20:15Z |
| Comments | **still working** — a contributor commented on #6706 on 2026-08-12 |

**Three pull requests were merged on 2026-08-07** — after the documented 2026-07-29 code freeze and
after the snapshot was taken:

| PR | Title | Merged | Author |
| --- | --- | --- | --- |
| #6680 | fix: upsert history permissions | 2026-08-07 | Ilango Rajagopal (maintainer) |
| #6708 | fix: flowise-603 | 2026-08-07 | Ilango Rajagopal (maintainer) |
| #6709 | fix: flowise-766 | 2026-08-07 | Ilango Rajagopal (maintainer) |

So the repository is **frozen but live**, not archived. Contributions can still be commented on and,
on this evidence, still merged.

**This contradicts statements in `docs/PROJECT-LOG.md`, `docs/product.md` and `docs/STATUS.md`**,
all of which assert the archive happened on 2026-08-10. Those need correcting — the fork's founding
narrative currently states something observably untrue.

It also means a public comment on #6706 saying "this repository is archived on 2026-08-10" is
wrong and should be corrected by whoever posted it.

## What changed since 2026-08-05

| | Snapshot | Now | Δ |
| --- | ---: | ---: | ---: |
| Open PRs | 347 | 346 | −1 |
| Open issues | 698 | 701 | +3 |

**New PRs (3)** — patches captured into `patches/`, all verified free of licensed-path hunks:

| PR | Author | Title |
| --- | --- | --- |
| #6714 | sarveshpatil-plivo | feat: add Plivo tool nodes and credential |
| #6723 | alexluisillo91-creator | feat: add TaskMarket discovery tools |
| #6725 | uuzzrm | fix(mcp): clarify missing remote server URL |

**Left the open set (4)** — #6680, #6708, #6709 merged; #6626 closed unmerged.

**20 PRs had activity** since the snapshot, so the backlog is not inert.

## We are missing three upstream fixes

The three merged PRs are **not in Flow-Wiser** — they landed upstream two days after the fork's
snapshot. Captured to `merged-after-snapshot/` as `git am`-able patches with authorship intact.

All three are maintainer-authored bug fixes, none touch commercially licensed paths:

- **#6680** — upsert history permissions (5 files)
- **#6708** — flowise-603 (7 files)
- **#6709** — flowise-766 (4 files)

**Recommended: adopt all three.** They are upstream-reviewed, upstream-merged fixes to code
Flow-Wiser shares. Apply with
`git am --keep-non-patch upstream-archive/merged-after-snapshot/pr-<n>.patch`.

## Where Flow-Wiser has already solved an open upstream PR

These are still open upstream while Flow-Wiser ships a fix for the same defect. Ordered by how
confident the match is.

### Confirmed — same defect, Flow-Wiser's fix is more complete

| Upstream PR | Their fix | Flow-Wiser |
| --- | --- | --- |
| **#6670** pin flowise version in Docker image (`iruzen-dono`, +3/−2) | pins the version in `docker/Dockerfile` | pins it as a **build arg** so it participates in the layer cache key, **plus** a post-install assertion that installed == requested, failing the build loudly on mismatch |
| **#6642** pin Docker image package version (`zcxGGmu`, +19/−2) | same defect, also touches the dockerhub workflow | as above |
| **#6706** connect-sqlite3 0.9.17 crash (`PiedPiper911`) | pins `0.9.16` for source builds | pins `0.9.16` **and** applies it at npm-install time for container builds, with a final gate after all mutations |
| **#4805** Node 22 support (`Maxiweb`, +3/−3) | changes 3 files: workflow, root and server `package.json` | ADR-0004 aligned **ten** locations, including three Dockerfile `ARG NODE_VERSION` defaults and five workflow values, and verified on both branch lines in CI |

**#6670 and #6642 are the fork's founding defect.** Two contributors independently reported the
unpinned Docker install; both PRs are still open. Flow-Wiser exists partly because that defect
shipped three mislabelled images.

**#4805 has been open since 2025-07-06** — over a year — for the change Flow-Wiser completed and
verified this week.

### Candidate — same area, needs comparison before any claim

| Upstream PR | Note |
| --- | --- |
| #6627 `HTTP_ALLOW_LIST` for trusted SSRF hosts (+513/−54, with tests) | Flow-Wiser has SSRF work incl. the IPv6 `::` gap and MCP transport guard. **Not yet compared** — do not assert equivalence without reading both |
| #5336 prevent SSRF and input type confusion | same |
| #6636 / #6610 / #6601 RediSearch special-character escaping | three overlapping PRs from different authors; Flow-Wiser status unknown |

## Recommended actions

1. **Correct the archive claim** in `PROJECT-LOG.md`, `product.md`, `STATUS.md` — and in the public
   comment on #6706. This is a factual error in the fork's own founding narrative.
2. **Adopt the three merged fixes** (#6680, #6708, #6709) with authorship preserved.
3. **Consider contributing back.** Upstream still merges. Flow-Wiser holds verified, more complete
   fixes for #6670, #6642, #6706 and #4805. Offering them upstream would help every Flowise user,
   not only fork users — and it costs nothing the fork needs to keep.
   **Any such contribution is an outward action and needs explicit human authorization.**
4. **Re-run this delta before relying on the archive again.** The 2026-08-05 capture was already
   stale within 48 hours.

## Capture notes

- New patches fetched from `github.com/FlowiseAI/Flowise/pull/<n>.patch`, preserving `From:` headers
  so `git am` retains original authorship.
- **Licence check applied**: all three new patches and all three merged patches were checked for
  `diff --git` headers against `packages/server/src/enterprise/` and `IdentityManager.ts`.
  **All six are clean** — no stripping was required, and no hunk content was read to determine that.
- `prs/index.json` and `issues/open-issues.json` were refreshed and **normalised to a single JSON
  array**. The originals were 11 concatenated per-page arrays, which parse only with `jq -s`. Data
  was complete in both; only the shape changed.
